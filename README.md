# 随机获取E621帖子&图片

**命令**：

- /random-image&lt;tags&gt;：指定关键词搜索随机帖子。**←** 这个命令有非常多别名，可以看源代码查。
- /rating：强制分级，防止bot发出NSFW内容。
- /constants：恒标签，每次搜随机图都会带，可以用于恶作剧（？

大模型可以通过`search_random_image`工具来调用随机图搜索。
